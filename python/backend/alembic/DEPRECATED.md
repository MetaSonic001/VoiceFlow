# DEPRECATED — do not use this directory

This `alembic/` directory is no longer active.  
`alembic.ini` points to `../migrations/` which is the canonical migration root.

All current migrations live in `migrations/versions/`.  
This folder can be safely deleted once the team confirms no CI scripts reference it.

# Example env file for DB Visualizer
# Postgres (preferred variable BACKEND_DATABASE_URL, or DATABASE_URL)
BACKEND_DATABASE_URL=postgresql+asyncpg://doc_user:doc_password@localhost:5433/documents_db

# MinIO / S3
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=voiceflow

# Chroma local persistence directory (optional)
CHROMA_PERSIST_DIR=./chroma_db

# Optional: if your backend app uses a different variable name
DATABASE_URL=${BACKEND_DATABASE_URL}
